{
  "task_id": "T-0002",
  "prompt": "Fix the bug in src/signals.py so that `pytest tests/ -x -q` passes. Touch ONLY files under src/.\n\nThe function normalize_probability(up_price, down_price) should return the UP-side probability: up_price divided by the sum of both prices. Currently it returns the wrong value. All 5 tests must pass.",
  "target_repo": "sandbox-py",
  "base_ref": "HEAD",
  "allowed_scope": [
    "src/**"
  ],
  "heldout_set": "T-0002",
  "class": "small",
  "complexity": 2,
  "spec_clarity": 5
}
